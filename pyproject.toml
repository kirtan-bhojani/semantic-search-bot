[project]
name = "semantic-search-bot"
version = "1.0.0"
description = "Semantic document search system with embedding-based retrieval and RAG question answering"
requires-python = ">=3.10"
license = {text = "MIT"}

[tool.pytest.ini_options]
testpaths = ["tests"]
